{
  "name": "sicherda",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create sicherda-db",
    "db:migrate": "wrangler d1 execute sicherda-db --file=./schema.sql",
    "db:local": "wrangler d1 execute sicherda-db --file=./schema.sql --local"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.0",
    "wrangler": "^4.76.0"
  }
}
