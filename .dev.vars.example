TURNSTILE_SITE_KEY=    # Optional lokal - localhost nutzt automatisch Cloudflare-Testwerte
TURNSTILE_SECRET_KEY=  # Pflicht fuer remote/Deploy - localhost nutzt automatisch das Test-Secret
DEV_TOKEN=          # Nur für lokale Entwicklung – niemals in Prod setzen
