name = "ibinda"
main = "src/index.ts"
compatibility_date = "2024-01-01"
account_id = "bcde28cac6ec64c3ed3a810c62971db5"

# D1 Datenbank
[[d1_databases]]
binding = "DB"
database_name = "dev-ibinda-db"
database_id = "c6136eb7-5dce-4c0c-b937-a47207b3f6ef"
migrations_dir = "migrations"

# Cron-Job für Überfälligkeits-Check
[triggers]
crons = ["* * * * *"] # Jede Minute

# Environment Variables (Secrets via wrangler secret)
[vars]
TURNSTILE_SITE_KEY = "0x4AAAAAACuh4_5jdfIdTeIC"
# EXPO_ACCESS_TOKEN = "" # Via wrangler secret put EXPO_ACCESS_TOKEN
# Secrets (nicht hier – via wrangler secret put):
#   TURNSTILE_SECRET_KEY
#   DEV_TOKEN (nur für Staging, nicht Prod)
